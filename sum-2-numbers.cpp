#include <iostream>

int main() {
  int firstNumber, secondNumber, res;
  std::cin >> firstNumber >> secondNumber;
  res = firstNumber + secondNumber;
  std::cout << "Sum of 2 numbers is: " << res << std::endl;
  return 0;
}
